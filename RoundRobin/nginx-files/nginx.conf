events {}

http{

    upstream fast_api {
        server localhost:8000 weight=2;
        server localhost:8001 weight=3;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://fast_api/;
        }
    }
}